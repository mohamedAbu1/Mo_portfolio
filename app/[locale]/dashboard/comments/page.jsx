"use client";import{useEffect,useState}from"react";import{useSession}from"next-auth/react";import{FaArrowLeft,FaCheck,FaXmark,FaTrash}from"react-icons/fa6";import Link from"next/link";
export default function CommentModeration(){const{data:session,status}=useSession();const[items,setItems]=useState([]);useEffect(()=>{if(status==="authenticated")fetch("/api/admin/comments").then(r=>r.json()).then(d=>setItems(d.data||[]))},[status]);async function act(id,method,status){const r=await fetch(`/api/admin/comments${method==="DELETE"?`?id=${id}`:""}`,{method,headers:method!=="DELETE"?{"Content-Type":"application/json"}:undefined,body:method!=="DELETE"?JSON.stringify({id,status}):undefined});if(r.ok)setItems(xs=>xs.filter(x=>x.id!==id))}if(status==="loading")return <main className="dashboard-page"><div className="dashboard-loading">Loading...</div></main>;if(!session||session.user?.role!=="admin")return <main className="dashboard-page"><div className="dashboard-loading"><h1>Access required</h1><Link href="/en/login" className="button primary">Sign in</Link></div></main>;return <main className="dashboard-page"><aside className="dashboard-sidebar"><Link className="brand" href="/"><img className="brand-logo" src="/images/mohamed-abu-logo.png" alt="Mohamed Abu"/></Link><nav className="comments-nav"><Link href="/en/dashboard"><FaArrowLeft/> Dashboard</Link></nav></aside><section className="dashboard-main"><header className="dashboard-header"><div><p className="eyebrow">MODERATION / COMMENTS</p><h1>Community notes.</h1></div></header><div className="moderation-list">{items.length===0?<div className="empty-state"><span>✓</span><p>Everything is reviewed.<br/>No comments waiting for approval.</p></div>:items.map(c=><article className="moderation-card" key={c.id}><img src={c.author_image||"/images/MyPic.webp"} alt=""/><div><strong>{c.author_name}</strong><small>{c.project_key} · {new Date(c.created_at).toLocaleString()}</small><p>{c.content}</p></div><div className="moderation-actions"><button onClick={()=>act(c.id,"PUT","approved")} title="Approve"><FaCheck/></button><button onClick={()=>act(c.id,"PUT","rejected")} title="Reject"><FaXmark/></button><button onClick={()=>act(c.id,"DELETE")} title="Delete"><FaTrash/></button></div></article>)}</div></section></main>}

